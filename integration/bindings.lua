-- Selection and clipboard shortcuts are managed by SayIt Settings.
-- The remaining player controls below are optional bindings.
-- F9 remains Voxtype push-to-talk.
-- Default selection shortcut: F10. Save Settings to apply it.
-- Clipboard shortcut is unassigned by default. Configure it in SayIt Settings.
o.bind("CTRL + F10", "Pause or resume speech", "sayit toggle")
o.bind("ALT + F10", "Stop speech", "sayit stop")
o.bind("SUPER + F10", "Speech player", "sayit ui")

